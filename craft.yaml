---
on:
  push:
    branches:
      - hcloud-test

project:
    id: 9
    name: hcloud-test

services:
  - name: web
    type: container
    build:
      repo: https://github.com/huma-engineering/hcloud-test
      branch: master
      dockerfile: build/Dockerfile
      context: web
    ingress:
      host: hcloud-test.sbx.huma.com
      paths: ["/"]
      port: 80
